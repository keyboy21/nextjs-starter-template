import type { PageComponent } from '@/types/component.types';

const PortfolioPage: PageComponent = async () => {
	return (
		<section>
			<h1>Portfolio</h1>
		</section>
	);
};

export default PortfolioPage;
